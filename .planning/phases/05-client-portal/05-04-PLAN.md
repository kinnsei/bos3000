---
phase: 05-client-portal
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - portal/src/features/cdr/index.tsx
  - portal/src/features/cdr/cdr-table.tsx
  - portal/src/features/cdr/cdr-detail-sheet.tsx
  - portal/src/features/cdr/recording-player.tsx
  - portal/src/features/cdr/cdr-export.tsx
  - portal/src/features/wastage/index.tsx
  - portal/src/features/wastage/wastage-trend.tsx
  - portal/src/features/wastage/wastage-detail.tsx
  - portal/src/features/wastage/failure-distribution.tsx
  - portal/src/lib/export.ts
autonomous: true
requirements:
  - CLNT-03
  - CLNT-05
  - WAST-04

must_haves:
  truths:
    - "Customer can query CDR list with filters (date range, status, number) and paginate results"
    - "Customer can export CDR results to Excel or CSV with Chinese column headers"
    - "Customer can click a CDR row to open side drawer showing A/B leg details, status timeline, recording player, and cost breakdown"
    - "Recording player plays A-leg, B-leg, and merged recordings from presigned S3 URLs"
    - "Customer can view wastage overview with trend chart and B-leg failure reason pie chart"
    - "Customer can view wastage detail list with individual wastage records"
  artifacts:
    - path: "portal/src/features/cdr/index.tsx"
      provides: "CDR query page with table, filters, and export"
      min_lines: 40
    - path: "portal/src/features/cdr/cdr-detail-sheet.tsx"
      provides: "CDR detail side drawer with A/B leg info, timeline, recordings, costs"
      min_lines: 80
    - path: "portal/src/features/cdr/recording-player.tsx"
      provides: "HTML5 audio player for recording playback"
      min_lines: 15
    - path: "portal/src/lib/export.ts"
      provides: "Excel and CSV export utilities using SheetJS"
      min_lines: 30
    - path: "portal/src/features/wastage/index.tsx"
      provides: "Wastage analysis page with trend, distribution, and detail"
      min_lines: 40
  key_links:
    - from: "portal/src/features/cdr/cdr-export.tsx"
      to: "portal/src/lib/export.ts"
      via: "Calls exportToExcel/exportToCSV"
      pattern: "exportTo(Excel|CSV)"
    - from: "portal/src/features/cdr/cdr-detail-sheet.tsx"
      to: "portal/src/features/cdr/recording-player.tsx"
      via: "Embeds RecordingPlayer for A/B/merged recordings"
      pattern: "RecordingPlayer"
    - from: "portal/src/features/cdr/cdr-table.tsx"
      to: "portal/src/components/shared/data-table.tsx"
      via: "Uses DataTable for CDR list"
      pattern: "DataTable"
---

<objective>
Build the CDR query/export/detail pages and Wastage analysis pages -- the customer's data investigation tools.

Purpose: CDR and Wastage are closely related data views. CDR lets customers investigate individual calls (with recording playback). Wastage lets customers understand failure patterns. Building them together ensures consistent data display patterns.
Output: CDR page with table, filters, export, detail sheet with recording player. Wastage page with trend chart, failure distribution, and detail list.
</objective>

<execution_context>
@/Users/kinnsei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kinnsei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-client-portal/05-CONTEXT.md
@.planning/phases/05-client-portal/05-RESEARCH.md
@.planning/phases/05-client-portal/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CDR query page with table, filters, export, and detail side drawer with recording player</name>
  <files>
    portal/src/features/cdr/index.tsx, portal/src/features/cdr/cdr-table.tsx,
    portal/src/features/cdr/cdr-detail-sheet.tsx, portal/src/features/cdr/recording-player.tsx,
    portal/src/features/cdr/cdr-export.tsx, portal/src/lib/export.ts
  </files>
  <action>
    1. Create lib/export.ts following RESEARCH.md Pattern 4:
       - `exportToExcel(data, columns, filename)`: SheetJS json_to_sheet with Chinese column headers, column widths, writes .xlsx file
       - `exportToCSV(data, columns, filename)`: SheetJS sheet_to_csv with BOM prefix for Chinese encoding, creates Blob and triggers download
       - Column definitions with Chinese headers: 通话ID, A号码, B号码, 状态, 时长(秒), 费用(元), 损耗类型, 发起时间

    2. Create features/cdr/recording-player.tsx following RESEARCH.md Pattern 5:
       - Props: url (string|null), label (string)
       - If no URL: show "暂无录音" muted text
       - HTML5 `<audio>` with controls, preload="none", full width, compact height
       - Label above player

    3. Create features/cdr/cdr-table.tsx:
       - Column definitions: 通话ID (truncated), A号码, B号码, 状态 (Badge with color), 时长, 费用, 发起时间 (date-fns format)
       - Uses DataTable with server-side pagination and sorting
       - Row click: opens CDR detail sheet
       - Toolbar: date range picker (shadcn Calendar in Popover), status select filter, number search input

    4. Create features/cdr/cdr-export.tsx:
       - Dropdown button: "导出" with two options: "导出 Excel", "导出 CSV"
       - On click: fetch current filtered results (up to 5000 rows) via API, call exportToExcel or exportToCSV
       - Show loading toast during export, success toast on completion
       - Warn if result set exceeds 5000: "数据量较大，建议缩小筛选范围"

    5. Create features/cdr/cdr-detail-sheet.tsx:
       - shadcn Sheet (right side drawer, wide ~600px)
       - Sections separated by dividers:
         - **基本信息**: Call ID, status badge, created_at, duration
         - **A 路信息**: A number, gateway, status, answer_time, hangup_cause
         - **B 路信息**: B number, gateway, status, answer_time, hangup_cause
         - **状态时间线**: Vertical timeline showing state transitions with timestamps (initiating -> a_dialing -> a_connected -> b_dialing -> bridged -> finished)
         - **录音播放**: Three RecordingPlayer instances (A路录音, B路录音, 合并录音)
         - **费用明细**: Pre-deduction amount, actual cost, refund amount, rate plan name, wastage cost (if any)
       - All labels in Chinese

    6. Create features/cdr/index.tsx:
       - Page header "话单查询" with CdrExport button
       - CdrTable below
       - CdrDetailSheet controlled by selected row state
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/portal && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - CDR table renders with filters (date, status, number) and pagination
    - Export dropdown produces Excel/CSV files with Chinese headers
    - Row click opens side drawer with full call detail
    - Recording player renders for A/B/merged recordings
    - Status timeline shows call lifecycle
    - Cost breakdown section displays billing details
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Wastage analysis page with trend chart, failure distribution, and detail list</name>
  <files>
    portal/src/features/wastage/index.tsx, portal/src/features/wastage/wastage-trend.tsx,
    portal/src/features/wastage/wastage-detail.tsx, portal/src/features/wastage/failure-distribution.tsx
  </files>
  <action>
    1. Create features/wastage/wastage-trend.tsx:
       - Recharts AreaChart showing wastage rate trend over time (7d/30d toggle)
       - X axis: dates, Y axis: wastage rate percentage
       - Secondary line: wastage cost in yuan
       - Use chart-theme.ts colors (red/orange for wastage)
       - Loading: Skeleton rectangle
       - Responsive container, Chinese axis labels and tooltip

    2. Create features/wastage/failure-distribution.tsx:
       - Recharts PieChart showing B-leg failure reason distribution
       - Segments: a_connected_b_failed, bridge_broken_early, b_no_answer, b_busy, b_rejected, other
       - Chinese labels for each reason: "A通B不通", "桥接秒断", "B路无应答", "B路忙", "B路拒接", "其他"
       - Legend below chart
       - Optional: Recharts Bar chart as alternative view showing counts by reason

    3. Create features/wastage/wastage-detail.tsx:
       - DataTable showing individual wastage records
       - Columns: 通话ID, A号码, B号码, 损耗类型 (Badge), 损耗费用, B路原因, 发起时间
       - Server-side pagination
       - Row click navigates to CDR detail (reuse CDR detail sheet or link to /cdr?id=xxx)

    4. Create features/wastage/index.tsx:
       - Page header "损耗分析"
       - Top section: Two cards side by side -- WastageTrend (wider) + FailureDistribution (narrower)
       - Bottom section: "损耗明细" heading + WastageDetail table
       - Period selector (7天/30天/自定义) affecting all three components
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/portal && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Wastage trend chart renders with 7d/30d toggle
    - Failure distribution pie chart shows Chinese reason labels
    - Wastage detail table with pagination
    - All sections respond to period selector
    - All labels in Chinese
  </done>
</task>

</tasks>

<verification>
- `cd portal && npx tsc --noEmit` passes
- CDR page at `/cdr` renders table with filters, export, and detail sheet
- Wastage page at `/wastage` renders trend chart, pie chart, and detail table
- Export button produces valid Excel/CSV files
</verification>

<success_criteria>
- CDR table supports filtering, pagination, sorting, and export (Excel/CSV)
- CDR detail sheet shows complete call information with recording player
- Wastage analysis shows trend, failure distribution, and detail list
- All Chinese labels and formatting throughout
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-portal/05-04-SUMMARY.md`
</output>
