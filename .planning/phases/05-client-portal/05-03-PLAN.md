---
phase: 05-client-portal
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - portal/src/features/dashboard/index.tsx
  - portal/src/features/dashboard/metric-cards.tsx
  - portal/src/features/dashboard/trend-chart.tsx
  - portal/src/features/callback/index.tsx
  - portal/src/features/callback/callback-form.tsx
  - portal/src/features/callback/call-card.tsx
  - portal/src/features/callback/active-calls.tsx
autonomous: true
requirements:
  - CLNT-01
  - CLNT-02

must_haves:
  truths:
    - "Customer sees dashboard with 4-6 metric cards (today's calls, success rate, wastage rate, spend, balance, concurrent)"
    - "Customer sees 1-2 trend charts below metric cards"
    - "Customer can fill callback form (A number + B number required) and submit to initiate a call"
    - "Active calls display as card list with A/B numbers, colored status badge, duration, hangup button"
    - "New call appears in active list immediately after initiation, WebSocket updates status in real-time"
  artifacts:
    - path: "portal/src/features/dashboard/index.tsx"
      provides: "Dashboard overview page with metrics and charts"
      min_lines: 40
    - path: "portal/src/features/callback/index.tsx"
      provides: "Callback operations center: form + active calls + history"
      min_lines: 50
    - path: "portal/src/features/callback/call-card.tsx"
      provides: "Active call card with status badge and hangup"
      min_lines: 30
    - path: "portal/src/features/callback/callback-form.tsx"
      provides: "Callback initiation form with Zod validation"
      min_lines: 40
  key_links:
    - from: "portal/src/features/callback/active-calls.tsx"
      to: "portal/src/hooks/use-call-ws.ts"
      via: "Reads ['calls', 'active'] query data populated by WebSocket"
      pattern: "useQuery.*calls.*active"
    - from: "portal/src/features/callback/callback-form.tsx"
      to: "portal/src/lib/api/hooks.ts"
      via: "useMutation for callback initiation"
      pattern: "useMutation"
    - from: "portal/src/features/dashboard/metric-cards.tsx"
      to: "portal/src/components/shared/stat-card.tsx"
      via: "Uses StatCard for each metric"
      pattern: "StatCard"
---

<objective>
Build the Dashboard overview page and Callback operations center -- the two highest-value customer-facing features.

Purpose: Dashboard is the landing page customers see on login. Callback is the primary "operations center" where customers initiate calls and monitor them in real-time via WebSocket. These two pages define the core Portal experience.
Output: Working dashboard with metrics/charts and callback page with form + real-time active call cards.
</objective>

<execution_context>
@/Users/kinnsei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kinnsei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-client-portal/05-CONTEXT.md
@.planning/phases/05-client-portal/05-RESEARCH.md
@.planning/phases/05-client-portal/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Dashboard overview page with metric cards and trend charts</name>
  <files>
    portal/src/features/dashboard/index.tsx,
    portal/src/features/dashboard/metric-cards.tsx,
    portal/src/features/dashboard/trend-chart.tsx
  </files>
  <action>
    1. Create features/dashboard/metric-cards.tsx:
       - Use useDashboardOverview() hook (from lib/api/hooks.ts) to fetch today's metrics
       - Render 6 StatCard components in a responsive grid (3 cols on lg, 2 on md, 1 on sm):
         - 今日呼叫 (PhoneCall icon, total calls count)
         - 成功率 (CheckCircle icon, bridge success percentage, green trend)
         - 损耗率 (TrendingDown icon, wastage percentage, red when high)
         - 今日消费 (DollarSign icon, total spend in yuan)
         - 账户余额 (Wallet icon, current balance)
         - 实时并发 (Activity icon, concurrent call count -- this updates via WebSocket invalidation)
       - Loading state: StatCard skeleton via loading prop
       - All labels in Chinese

    2. Create features/dashboard/trend-chart.tsx:
       - Render 1-2 Recharts AreaChart(s) showing recent trends:
         - Primary chart: 7-day call volume trend (area chart with gradient fill, using chart-theme colors)
         - Optional secondary: 7-day spend trend (line chart)
       - Use useDashboardOverview() or a dedicated useDashboardTrend() hook
       - Apply chart-theme.ts CHART_COLORS for consistent styling
       - Responsive container, proper Chinese axis labels, formatted tooltips
       - Loading: Skeleton rectangle placeholder

    3. Create features/dashboard/index.tsx:
       - Page layout: MetricCards at top, TrendChart(s) below
       - Section headers in Chinese: "今日概览", "趋势分析"
       - Clean spacing, consistent with Vercel Dashboard aesthetic
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/portal && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Dashboard renders 6 metric cards in responsive grid
    - Trend chart renders with Recharts AreaChart
    - Loading skeletons display while data fetches
    - All labels in Chinese
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Callback operations center with form, active call cards, and recent history</name>
  <files>
    portal/src/features/callback/index.tsx,
    portal/src/features/callback/callback-form.tsx,
    portal/src/features/callback/call-card.tsx,
    portal/src/features/callback/active-calls.tsx
  </files>
  <action>
    1. Create features/callback/callback-form.tsx:
       - react-hook-form + Zod schema: aNumber (required, phone pattern), bNumber (required, phone pattern)
       - Expandable "高级选项" section (shadcn Collapsible or manual toggle): optional fields for caller_id override, max_duration
       - On submit: useMutation calling callback initiate API, toast success ("回拨已发起"), toast error on failure
       - Disable submit button while mutation is pending (show spinner)
       - Form resets after successful submission
       - Chinese labels: "A 号码 (主叫)", "B 号码 (被叫)", "发起回拨"

    2. Create features/callback/call-card.tsx:
       - Following the exact pattern from RESEARCH.md CallCard component
       - Props: callId, aNumber, bNumber, status, durationSec, onHangup
       - Status badge with colored backgrounds: initiating (gray), a_dialing (yellow), a_connected (blue), b_dialing (cyan), bridged (green)
       - Chinese status labels: 发起中, A路拨号, A路已接, B路拨号, 通话中
       - Duration display: MM:SS format, ticking up for bridged calls (use a 1s interval for bridged status)
       - Hangup button (destructive variant) with ConfirmDialog: "确认挂断此通话？"

    3. Create features/callback/active-calls.tsx:
       - Read ['calls', 'active'] query data (populated by WebSocket hook in layout)
       - Render CallCard list, sorted by most recent first
       - Empty state: "当前无进行中的通话" with illustration
       - Hangup handler: useMutation calling callback hangup API, invalidate ['calls', 'active'] on success

    4. Create features/callback/index.tsx -- the "Operations Center" page:
       - Top section: CallbackForm
       - Divider
       - Middle section: "进行中通话" heading + ActiveCalls
       - Bottom section: "最近回拨记录" heading + DataTable showing recent 20 callback records (call_id, A/B numbers, status badge, duration, created_at). Use a useRecentCallbacks() query hook.
       - Single page, no tabs -- all three sections visible
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/portal && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Callback form validates and submits with A/B number fields
    - Active call cards display with colored status badges and duration
    - Hangup button triggers confirmation dialog then API call
    - WebSocket events update active call cards in real-time
    - Recent callback history table shows below active calls
    - All labels and interactions in Chinese
  </done>
</task>

</tasks>

<verification>
- `cd portal && npx tsc --noEmit` passes
- Dashboard page renders at `/` with metric cards and chart
- Callback page renders at `/callback` with form + active calls + history
- TypeScript compiles without errors
</verification>

<success_criteria>
- Dashboard shows 6 metric cards with loading skeletons and trend charts
- Callback form validates A/B phone numbers and submits
- Active call cards update in real-time via WebSocket
- Hangup button works with confirmation dialog
- Recent callback history displayed in DataTable
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-portal/05-03-SUMMARY.md`
</output>
