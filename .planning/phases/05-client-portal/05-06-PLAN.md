---
phase: 05-client-portal
plan: 06
type: execute
wave: 4
depends_on: ["05-03", "05-04", "05-05"]
files_modified:
  - admin/src/hooks/use-call-ws.ts
  - admin/src/components/shared/ws-status-banner.tsx
  - admin/src/app/layout.tsx
  - admin/src/features/cdr/live-calls.tsx
autonomous: false
requirements:
  - UI-04

must_haves:
  truths:
    - "Admin Dashboard connects to WebSocket for live call monitoring (replacing 30s polling)"
    - "Admin receives ALL call status events (not user-scoped)"
    - "Admin live call monitoring page updates in real-time via WebSocket"
    - "Admin shows disconnect banner when WebSocket connection is lost"
    - "Portal visual quality matches Admin Dashboard commercial-grade aesthetics"
  artifacts:
    - path: "admin/src/hooks/use-call-ws.ts"
      provides: "WebSocket hook for Admin (same pattern as Portal, no user scoping)"
      min_lines: 40
    - path: "admin/src/components/shared/ws-status-banner.tsx"
      provides: "Connection status banner for Admin"
      min_lines: 10
  key_links:
    - from: "admin/src/app/layout.tsx"
      to: "admin/src/hooks/use-call-ws.ts"
      via: "useCallStatusWebSocket() in Admin layout"
      pattern: "useCallStatusWebSocket"
    - from: "admin/src/features/cdr/live-calls.tsx"
      to: "@tanstack/react-query"
      via: "Reads ['calls', 'active'] query cache populated by WebSocket"
      pattern: "useQuery.*calls.*active"
---

<objective>
Upgrade Admin Dashboard to WebSocket (replacing 30s polling for live calls) and do a visual polish pass on the Portal with human verification.

Purpose: UI-04 requires WebSocket for BOTH Admin and Client dashboards. The Portal WebSocket was built in Plan 02. This plan adds WebSocket to Admin and verifies overall visual quality. The checkpoint ensures both dashboards meet commercial-grade standards before phase completion.
Output: Admin with WebSocket live call updates + verified Portal visual quality.
</objective>

<execution_context>
@/Users/kinnsei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kinnsei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-client-portal/05-CONTEXT.md
@.planning/phases/05-client-portal/05-RESEARCH.md
@.planning/phases/05-client-portal/05-02-SUMMARY.md
@.planning/phases/05-client-portal/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WebSocket hook and connection banner to Admin Dashboard, replace polling in live call monitoring</name>
  <files>
    admin/src/hooks/use-call-ws.ts, admin/src/components/shared/ws-status-banner.tsx,
    admin/src/app/layout.tsx, admin/src/features/cdr/live-calls.tsx
  </files>
  <action>
    1. Create admin/src/hooks/use-call-ws.ts:
       - Same pattern as Portal's use-call-ws.ts but adapted for Admin auth:
         - Admin uses JWT Cookie auth, but WebSocket needs query param token. Read the admin token from cookie or a stored session value.
         - If Admin stores a session token accessible to JS, use it. Otherwise, add a `/api/auth/ws-token` endpoint call that returns a short-lived WS token (fetched once on mount).
         - Connect to same `/ws/calls` endpoint. Admin connections receive ALL events (backend handles this based on isAdmin claim).
       - Same reconnect logic: exponential backoff 1s->30s max
       - Same cache updates: setQueryData on ['calls', 'active'], invalidateQueries on ['dashboard'] for completions

    2. Create admin/src/components/shared/ws-status-banner.tsx:
       - Identical to Portal's version: amber banner "实时连接已断开，正在重连..."
       - Returns null when connected

    3. Modify admin/src/app/layout.tsx:
       - Add useCallStatusWebSocket() call at layout level
       - Add WSStatusBanner below topnav (same position as Portal)
       - Pass isConnected to banner

    4. Modify admin/src/features/cdr/live-calls.tsx (or equivalent live monitoring component):
       - Remove refetchInterval polling (30s) for active calls query
       - Instead, read ['calls', 'active'] query data (now populated by WebSocket)
       - Keep a manual "刷新" button that invalidates ['calls', 'active'] as fallback
       - Active calls should now update in real-time without polling

    NOTE: The Admin Dashboard was built in Phase 4. These modifications are minimal -- adding 2 new files and modifying 2 existing files. The existing live call monitoring already renders call data; we're just changing the data source from polling to WebSocket.
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/admin && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Admin layout includes WebSocket hook and disconnect banner
    - Live call monitoring no longer polls (removed refetchInterval)
    - Active calls update via WebSocket events
    - Manual refresh button still works as fallback
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Portal and Admin visual quality and WebSocket functionality</name>
  <files>portal/src/app/layout.tsx, admin/src/app/layout.tsx</files>
  <action>
    Human visual verification of the complete Client Portal and Admin WebSocket upgrade.
    Start both dev servers and walk through all Portal pages and Admin WebSocket integration.
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/portal && npx tsc --noEmit && cd /Users/kinnsei/ai/bos3000/admin && npx tsc --noEmit</automated>
  </verify>
  <done>Human approves visual quality and functional completeness of both Portal and Admin WebSocket</done>
  <what-built>
    Complete Client Portal (all 7 feature pages) and Admin WebSocket upgrade:
    - Portal: Dashboard overview, Callback operations, CDR query/export/detail, Wastage analysis, Finance center, API integration, Account settings
    - WebSocket real-time push for both Admin and Portal
    - Identical Vercel-style visual quality between Admin and Portal
  </what-built>
  <how-to-verify>
    1. Start Portal: `cd portal && npm run dev` (opens at localhost:5174)
    2. Start Admin: `cd admin && npm run dev` (opens at localhost:5173)
    3. Check Portal login page: should show "BOS3000 客户门户" with same styling as Admin
    4. Navigate Portal sidebar: verify all 7 menu groups render (概览/业务/账务/开发/设置)
    5. Check Dashboard: 6 metric cards in row, trend chart below
    6. Check Callback page: form at top, active calls section, recent history table
    7. Check CDR page: table with filters, click row to see detail sheet with recording player
    8. Check Wastage page: trend chart + pie chart + detail table
    9. Check Finance: three tabs (overview, transactions, rates)
    10. Check API Integration: API key, webhook config with test button, IP whitelist
    11. Check Settings: profile tab, security tab
    12. Toggle dark/light theme: verify consistent across both Admin and Portal
    13. Check disconnect banner: appears when WebSocket backend is not running
    14. Overall: visual quality should feel like same product as Admin Dashboard
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Admin TypeScript compiles without errors after WebSocket additions
- Portal TypeScript compiles without errors
- Both dashboards connect to same WebSocket endpoint
- Visual consistency between Admin and Portal verified by human
</verification>

<success_criteria>
- Admin live call monitoring uses WebSocket instead of polling
- Both Admin and Portal show disconnect banner when WS is down
- All Portal feature pages render correctly with professional aesthetics
- Human approves visual quality and functional completeness
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-portal/05-06-SUMMARY.md`
</output>
