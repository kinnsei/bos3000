---
phase: 05-client-portal
plan: 05
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - portal/src/features/finance/index.tsx
  - portal/src/features/finance/balance-card.tsx
  - portal/src/features/finance/transactions-table.tsx
  - portal/src/features/finance/rate-query.tsx
  - portal/src/features/api-integration/index.tsx
  - portal/src/features/api-integration/api-keys.tsx
  - portal/src/features/api-integration/webhook-config.tsx
  - portal/src/features/api-integration/ip-whitelist.tsx
  - portal/src/features/settings/index.tsx
  - portal/src/features/settings/profile-form.tsx
  - portal/src/features/settings/security-settings.tsx
autonomous: true
requirements:
  - CLNT-04
  - CLNT-06
  - CLNT-07

must_haves:
  truths:
    - "Customer can view balance, credit limit, and transaction history"
    - "Customer can query rates by entering a phone number prefix"
    - "Customer can view API Key prefix and reset the key (with confirmation)"
    - "Customer can configure webhook URL and test it with a sample payload"
    - "Customer can manage IP whitelist entries (add/remove)"
    - "Customer can update profile, change password, and view DID pool"
  artifacts:
    - path: "portal/src/features/finance/index.tsx"
      provides: "Finance center with balance, transactions, rates"
      min_lines: 30
    - path: "portal/src/features/api-integration/index.tsx"
      provides: "API integration page with keys, webhook, IP whitelist"
      min_lines: 30
    - path: "portal/src/features/api-integration/webhook-config.tsx"
      provides: "Webhook configuration with test functionality"
      min_lines: 50
    - path: "portal/src/features/settings/index.tsx"
      provides: "Account settings with profile, security, DID pool"
      min_lines: 30
  key_links:
    - from: "portal/src/features/finance/transactions-table.tsx"
      to: "portal/src/components/shared/data-table.tsx"
      via: "Uses DataTable for transaction history"
      pattern: "DataTable"
    - from: "portal/src/features/api-integration/api-keys.tsx"
      to: "portal/src/components/shared/confirm-dialog.tsx"
      via: "Confirmation before API key reset"
      pattern: "ConfirmDialog"
---

<objective>
Build Finance center, API Integration, and Account Settings pages -- the customer self-service and configuration features.

Purpose: These three features complete the Portal's non-call functionality. Finance shows money. API Integration enables developer self-service. Settings manages the account. They share no complex dependencies and are straightforward CRUD/display pages.
Output: Three fully functional feature pages with forms, tables, and configuration management.
</objective>

<execution_context>
@/Users/kinnsei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kinnsei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-client-portal/05-CONTEXT.md
@.planning/phases/05-client-portal/05-RESEARCH.md
@.planning/phases/05-client-portal/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Finance center with balance display, transaction history, and rate query</name>
  <files>
    portal/src/features/finance/index.tsx, portal/src/features/finance/balance-card.tsx,
    portal/src/features/finance/transactions-table.tsx, portal/src/features/finance/rate-query.tsx
  </files>
  <action>
    1. Create features/finance/balance-card.tsx:
       - Two StatCard components side by side: 账户余额 (Wallet icon, large yuan amount) and 信用额度 (CreditCard icon)
       - Use useBalance() hook
       - Below cards: alert banner if balance < 100 yuan: "余额不足，请及时充值" (amber warning)

    2. Create features/finance/transactions-table.tsx:
       - DataTable for transaction history
       - Columns: 交易ID, 类型 (Badge: 预扣/结算/充值/退款), 金额 (red for deductions, green for credits), 余额变动, 关联通话ID, 时间
       - Server-side pagination, date range filter
       - Chinese labels throughout

    3. Create features/finance/rate-query.tsx:
       - Input field for phone number prefix (e.g., "138")
       - Submit button "查询费率"
       - Result display: Card showing matched rate plan name, A-leg rate, B-leg rate, billing unit (e.g., "6秒"), effective date
       - "未找到匹配费率" message if no match
       - Uses a useRateQuery(prefix) hook

    4. Create features/finance/index.tsx:
       - Page header "财务中心"
       - Tabs (shadcn Tabs): "账户概览", "消费流水", "费率查询"
       - Tab 1: BalanceCard
       - Tab 2: TransactionsTable
       - Tab 3: RateQuery
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/portal && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Finance page shows balance and credit limit cards
    - Low balance warning displays when applicable
    - Transaction history table with pagination and date filter
    - Rate query returns matching rate plan details
    - All Chinese labels
  </done>
</task>

<task type="auto">
  <name>Task 2: Build API Integration page and Account Settings page</name>
  <files>
    portal/src/features/api-integration/index.tsx, portal/src/features/api-integration/api-keys.tsx,
    portal/src/features/api-integration/webhook-config.tsx, portal/src/features/api-integration/ip-whitelist.tsx,
    portal/src/features/settings/index.tsx, portal/src/features/settings/profile-form.tsx,
    portal/src/features/settings/security-settings.tsx
  </files>
  <action>
    **API Integration:**

    1. Create features/api-integration/api-keys.tsx:
       - Display current API Key prefix (e.g., "bos_xxxx****") with copy button
       - "重置密钥" button with ConfirmDialog: "重置后旧密钥将立即失效，确认重置？"
       - On reset: useMutation, show new full key ONCE in a dialog with copy button (warn: "请保存此密钥，关闭后将无法再次查看")
       - Display key creation date

    2. Create features/api-integration/webhook-config.tsx:
       - Form (react-hook-form + Zod): webhook_url (URL validation), secret (read-only, with copy)
       - "保存" button saves webhook config
       - "发送测试" button: triggers test webhook delivery, shows response in a Sheet/Dialog:
         - HTTP status code, response body (truncated), delivery time
         - Success: green checkmark. Failure: red X with error message
       - Recent webhook deliveries: small DataTable below (last 20): event_type, status (Badge), response_code, sent_at

    3. Create features/api-integration/ip-whitelist.tsx:
       - List of current IP addresses with "删除" button (with confirmation)
       - "添加 IP" input + button at top
       - Zod validation for valid IPv4/IPv6
       - Empty state: "未配置 IP 白名单（所有 IP 可访问）"

    4. Create features/api-integration/index.tsx:
       - Page header "API 集成"
       - Three sections with Card wrappers: API 密钥, Webhook 配置, IP 白名单
       - Vertically stacked, each in its own Card

    **Account Settings:**

    5. Create features/settings/profile-form.tsx:
       - react-hook-form: company name (read-only), contact name (editable), email (editable), phone (editable)
       - "保存修改" button with useMutation
       - Read-only DID pool section: list of assigned DID numbers in a scroll area (no edit capability)

    6. Create features/settings/security-settings.tsx:
       - Change password form: current password, new password, confirm password
       - Zod validation: min 8 chars, must match confirmation
       - "修改密码" button with useMutation
       - Notification preferences section (placeholder for v1): checkboxes for email notifications (余额不足告警, 每日报告) -- just UI, backend TBD

    7. Create features/settings/index.tsx:
       - Page header "账户设置"
       - Tabs: "基本资料", "安全设置"
       - Tab 1: ProfileForm with DID pool section
       - Tab 2: SecuritySettings
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/portal && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - API Key display with masked prefix and reset functionality
    - Webhook config form with test button and delivery log
    - IP whitelist add/remove with validation
    - Profile form with editable fields and read-only DID list
    - Password change with validation
    - All Chinese labels and confirmations
  </done>
</task>

</tasks>

<verification>
- `cd portal && npx tsc --noEmit` passes
- Finance page at `/finance` with three tabs (overview, transactions, rates)
- API Integration page at `/api-integration` with key, webhook, IP sections
- Settings page at `/settings` with profile and security tabs
</verification>

<success_criteria>
- Finance: balance display, transaction history with pagination, rate query
- API Integration: key management with reset, webhook config with test, IP whitelist CRUD
- Settings: profile editing, password change, DID pool view
- All forms validate with Chinese error messages
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-portal/05-05-SUMMARY.md`
</output>
