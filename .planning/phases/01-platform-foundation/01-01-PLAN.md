---
phase: 01-platform-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/errcode/codes.go
  - pkg/errcode/codes_test.go
  - pkg/types/types.go
  - docker-compose.dev.yml
autonomous: true
requirements: [INFR-01, INFR-02, INFR-04]

must_haves:
  truths:
    - "Business error codes are available as typed constants with structured error creation"
    - "Shared money/phone types enforce integer arithmetic for monetary values"
    - "docker-compose.dev.yml starts a FreeSWITCH dev environment with one command"
    - "Encore app runs on port 12345 via documented CLI flag"
  artifacts:
    - path: "pkg/errcode/codes.go"
      provides: "Business error constants and NewError helper"
      exports: ["NewError", "ErrDetails", "InsufficientBalance", "BlacklistedNumber", "RateLimitExceeded", "NoHealthyGateway", "InvalidCredentials", "APIKeyRevoked", "IPNotWhitelisted", "PrefixNotFound", "DailyLimitExceeded", "ConcurrencyLimitExceeded"]
    - path: "pkg/errcode/codes_test.go"
      provides: "Tests for error code formatting"
      min_lines: 30
    - path: "pkg/types/types.go"
      provides: "Shared types (Money as int64, PhoneNumber)"
      exports: ["Money", "PhoneNumber"]
    - path: "docker-compose.dev.yml"
      provides: "FreeSWITCH dev environment"
      contains: "freeswitch"
  key_links:
    - from: "pkg/errcode/codes.go"
      to: "encore.dev/beta/errs"
      via: "import"
      pattern: "errs\\.Error"
---

<objective>
Create the shared foundation packages and infrastructure configuration that all Phase 1 services depend on.

Purpose: Establish error code conventions, shared types, and dev environment so all service plans (02-05) have consistent patterns to build against.
Output: pkg/errcode, pkg/types, docker-compose.dev.yml
</objective>

<execution_context>
@/Users/kinnsei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kinnsei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-foundation/01-CONTEXT.md
@.planning/phases/01-platform-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared error codes and types packages</name>
  <files>pkg/errcode/codes.go, pkg/errcode/codes_test.go, pkg/types/types.go</files>
  <behavior>
    - NewError(errs.FailedPrecondition, InsufficientBalance, "insufficient balance") produces an *errs.Error with Code=FailedPrecondition, Message="insufficient balance", Details containing BizCode="INSUFFICIENT_BALANCE" and a non-empty Suggestion
    - All business error constants are exported string constants matching SCREAMING_SNAKE_CASE format
    - Money type is int64 (fen/cents), PhoneNumber is string
    - ErrDetails struct has BizCode and Suggestion fields with json tags
  </behavior>
  <action>
Create `pkg/errcode/codes.go`:
- Package `errcode`, import `encore.dev/beta/errs`
- Define all business error code constants as per CONTEXT.md locked decision (English constants: INSUFFICIENT_BALANCE, BLACKLISTED_NUMBER, RATE_LIMIT_EXCEEDED, NO_HEALTHY_GATEWAY, INVALID_CREDENTIALS, API_KEY_REVOKED, IP_NOT_WHITELISTED, PREFIX_NOT_FOUND, DAILY_LIMIT_EXCEEDED, CONCURRENCY_LIMIT_EXCEEDED)
- ErrDetails struct with `BizCode string json:"biz_code"` and `Suggestion string json:"suggestion"`
- NewError(code errs.ErrCode, bizCode string, message string) error -- creates *errs.Error with Details containing ErrDetails. Include a suggestFor() helper that maps biz codes to actionable suggestions (e.g., INSUFFICIENT_BALANCE -> "Please top up your account balance")
- Error response follows Encore default: success returns data directly, failure returns {code, message, details}

Create `pkg/errcode/codes_test.go`:
- TestNewError: verify Code, Message, and Details fields
- TestAllConstantsFormat: verify all constants match SCREAMING_SNAKE pattern
- TestSuggestions: verify each biz code has a non-empty suggestion

Create `pkg/types/types.go`:
- Package `types`
- `type Money int64` with doc comment explaining fen/cents integer storage
- `type PhoneNumber string` with doc comment
- Helper: `func CeilDiv(a, b int64) int64` for billing block rounding
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000 && encore test ./pkg/...</automated>
  </verify>
  <done>All error code constants defined, NewError produces correct structured errors, Money/PhoneNumber types exist, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create docker-compose.dev.yml and document port configuration</name>
  <files>docker-compose.dev.yml</files>
  <action>
Create `docker-compose.dev.yml` at project root:
- FreeSWITCH service using `signalwire/freeswitch-public-release:1.10` (or `drachtio/drachtio-freeswitch-mrf:latest` if unavailable)
- Map SIP port 5060:5060/udp, ESL port 8021:8021/tcp
- Environment variables via entrypoint injection: ESL_PASSWORD=ClueCon (default), SIP_PORT=5060
- Volume mount for custom config: `./docker/freeswitch/conf:/etc/freeswitch` (optional, with comment)
- Health check: `nc -z localhost 8021` every 10s
- Network: bridge (default)
- Add comment at top: `# Start with: docker compose -f docker-compose.dev.yml up -d`
- Add comment: `# Encore app: encore run --listen :12345` (INFR-01 port config)

Note: INFR-01 requires port 12345. Encore uses `encore run --listen :12345` for local dev. Document this in the docker-compose file comments since it's a CLI flag, not a docker service.
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000 && docker compose -f docker-compose.dev.yml config --quiet 2>&1 && echo "docker-compose valid"</automated>
  </verify>
  <done>docker-compose.dev.yml validates, contains FreeSWITCH with ESL port 8021, port 12345 usage documented</done>
</task>

</tasks>

<verification>
- `encore test ./pkg/...` passes with all error code and types tests green
- `docker compose -f docker-compose.dev.yml config` validates without errors
- pkg/errcode/codes.go exports all required business error constants
- pkg/types/types.go exports Money and PhoneNumber types
</verification>

<success_criteria>
- All business error codes defined as constants with suggestions
- NewError helper creates properly structured Encore errors with Details
- Money stored as int64 (no float), PhoneNumber as string
- docker-compose.dev.yml is valid and starts FreeSWITCH with ESL access
- Port 12345 usage documented for `encore run`
</success_criteria>

<output>
After completion, create `.planning/phases/01-platform-foundation/01-01-SUMMARY.md`
</output>
