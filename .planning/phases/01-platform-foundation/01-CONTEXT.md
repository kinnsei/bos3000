# Phase 1: 平台基础 - Context

**Gathered:** 2026-03-09
**Status:** Ready for planning

<domain>
## 阶段范围

平台具备完整的用户认证、余额管理、费率计算、路由选择和合规检查能力。所有后续业务逻辑（呼叫引擎、录音、Webhook、前端）在此基础上构建。

覆盖需求：AUTH-01~03, BILL-01~05, ROUT-01~04, COMP-01~03, INFR-01~04

</domain>

<decisions>
## 实现决策

### 计费规则
- A 路按 6 秒块计费（不足 6 秒按 6 秒计），B 路按 60 秒块计费（不足 60 秒按 60 秒计）
- 费率模板支持两种计费精度，管理员按客户选择
- 呼叫发起时按固定时长（30 分钟）预扣余额，通话结束后精确结算，多退少补
- 客户只看到一笔总费用（单价×时长），管理员后台可看 A/B 路分别的成本明细和毛利分析
- 费率模板支持两种模式：统一单价（所有目的地同价）和按前缀计价（不同运营商前缀不同价格），管理员按客户选择模式

### API 响应与错误码
- 错误响应包含：业务错误码 + 可读描述 + 处理建议，不暴露内部实现细节（如网关名称、内部 ID）
- 业务错误码采用英文常量格式（如 INSUFFICIENT_BALANCE、BLACKLISTED_NUMBER、RATE_LIMIT_EXCEEDED）
- 错误信息仅英文，前端负责国际化
- API 响应结构遵循 Encore 默认规范：成功直接返回数据，失败返回 {code, message, details}

### 合规与限制策略
- 黑名单采用精确号码匹配（完整手机号），不支持前缀或正则
- 黑名单双层结构：先查全局黑名单（管理员维护，不可覆盖），再查客户级黑名单
- 日呼叫量限制按每客户统一限额（daily_limit 字段），管理员可按客户调整
- 审计日志保留 90 天，过期自动清理

### 号码与路由管理
- DID 外显号选取优先级：客户专属号码池 > 公共号码池随机选取
- B 路前缀表由管理员在网关管理界面手动维护（中国手机号前缀相对稳定）
- A 路网关采用简单整数权重轮询（如 weight=3），按比例分配流量
- 网关容灾基于定时健康检查，连续 N 次失败标记不健康，路由时自动跳过，恢复后自动重新加入

### Claude 自行决定
- JWT Token 有效期和刷新策略
- API Key 的格式和生成方式
- 健康检查的具体间隔和失败阈值
- 预扣金额的具体计算公式
- 数据库 schema 的字段级设计
- 审计日志的清理机制实现方式

</decisions>

<specifics>
## 具体想法

- A 路大多是 6 秒计费，B 路大多是分钟计费——这是行业惯例，系统需要同时支持两种计费精度
- 费率体系要灵活：有的客户统一单价，有的客户需要按运营商分别定价

</specifics>

<code_context>
## 现有代码情况

### 可复用资产
- 无现有代码，项目为全新 Encore.go 应用（仅有 encore.app 和 go.mod）

### 已建立的模式
- Encore.go 框架规范：服务定义、API 注解、sqldb、errs 包、auth handler
- Go 1.25.6 + encore.dev v1.52.1

### 集成点
- 全新项目，无需对接现有代码
- 后续阶段（Phase 2 呼叫引擎）将直接依赖本阶段的认证、计费、路由、合规模块

</code_context>

<deferred>
## 延后的想法

无——讨论保持在阶段范围内

</deferred>

---

*Phase: 01-platform-foundation*
*Context gathered: 2026-03-09*
