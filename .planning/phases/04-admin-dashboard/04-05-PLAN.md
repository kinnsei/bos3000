---
phase: 04-admin-dashboard
plan: 05
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - admin/src/features/cdr/index.tsx
  - admin/src/features/cdr/components/cdr-table.tsx
  - admin/src/features/cdr/components/cdr-filters.tsx
  - admin/src/features/cdr/components/live-calls.tsx
  - admin/src/features/finance/index.tsx
  - admin/src/features/finance/components/transaction-table.tsx
  - admin/src/features/finance/components/rate-plan-sheet.tsx
  - admin/src/features/finance/components/profit-analysis.tsx
  - admin/src/lib/api/hooks.ts
autonomous: true
requirements:
  - ADMN-04
  - ADMN-06

must_haves:
  truths:
    - "Admin can query all CDR records across users with date range, customer, status, and gateway filters"
    - "Admin can view live calls and force-hangup any active call with red confirmation"
    - "Admin can view all platform transactions with filters"
    - "Admin can create/edit rate plan templates via Sheet form"
    - "Admin can view profit analysis by customer and by gateway (Recharts)"
  artifacts:
    - path: "admin/src/features/cdr/index.tsx"
      provides: "CDR management page with tabs (history + live)"
      min_lines: 30
    - path: "admin/src/features/finance/index.tsx"
      provides: "Finance center page"
      min_lines: 30
    - path: "admin/src/features/finance/components/profit-analysis.tsx"
      provides: "Profit analysis charts"
      min_lines: 40
  key_links:
    - from: "admin/src/features/cdr/components/live-calls.tsx"
      to: "admin/src/lib/api/hooks.ts"
      via: "useForceHangup mutation"
      pattern: "useForceHangup"
    - from: "admin/src/features/finance/components/profit-analysis.tsx"
      to: "recharts"
      via: "BarChart for profit by customer/gateway"
      pattern: "BarChart"
---

<objective>
Build the CDR management module (full call record query, live call monitoring with force-hangup) and the finance center (transaction ledger, rate plan templates, profit analysis).

Purpose: CDR is the operational monitoring backbone; finance center is critical for revenue management and customer billing configuration.
Output: Two feature pages with DataTables, filters, and Recharts profit visualizations.
</objective>

<execution_context>
@/Users/kinnsei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kinnsei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-dashboard/04-CONTEXT.md
@.planning/phases/04-admin-dashboard/04-RESEARCH.md
@.planning/phases/04-admin-dashboard/04-01-SUMMARY.md
@.planning/phases/04-admin-dashboard/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CDR management -- full query with filters + live call monitoring with force-hangup</name>
  <files>
    admin/src/features/cdr/index.tsx,
    admin/src/features/cdr/components/cdr-table.tsx,
    admin/src/features/cdr/components/cdr-filters.tsx,
    admin/src/features/cdr/components/live-calls.tsx,
    admin/src/lib/api/hooks.ts
  </files>
  <action>
    1. Create cdr-filters.tsx:
       - Filter bar above CDR table with:
         a. Date range picker (shadcn Calendar + Popover, default: today)
         b. Customer select (dropdown, all customers)
         c. Status select (all, completed, failed, in-progress)
         d. Gateway select (dropdown)
         e. Phone number search (caller or callee)
       - Filters apply as query params to useCDRRecords() hook
       - Chinese labels for all filters

    2. Create cdr-table.tsx:
       - Columns: call ID (truncated), customer name, A-leg number, B-leg number, status (Badge), start time, duration (formatted mm:ss), gateway, cost (CNY), wastage type (if any), hangup cause
       - Server-side pagination, sorting by start_time/duration/cost
       - Row click expands detail panel showing: full A/B leg timeline, recording links (placeholder), raw hangup cause code
       - Export button (placeholder -- generates CSV of current filter results)

    3. Create live-calls.tsx:
       - DataTable showing only active/in-progress calls
       - Columns: call ID, customer, A-leg status, B-leg status, duration (live counter), gateway
       - Auto-refresh every 5 seconds (refetchInterval: 5000)
       - Force hangup button on each row: red AlertDialog per user decision ("Confirm force-hangup call X? This will disconnect both parties.")
       - After hangup, invalidate live calls and CDR queries

    4. Create cdr/index.tsx:
       - Tabs: "Call Records" and "Live Calls" (Chinese: "通话记录", "实时通话")
       - CDR tab: CdrFilters + CdrTable
       - Live tab: LiveCalls
       - Summary stats at top: total calls today, success rate, average duration

    5. Update hooks.ts: Add CDR hooks:
       - useCDRRecords({page, limit, dateRange, customerId, status, gatewayId, phoneSearch, sortBy, sortOrder})
       - useLiveCalls() with refetchInterval: 5000
       - useForceHangup() mutation with invalidation of ['live-calls', 'cdr']
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/admin && npx tsc --noEmit && npx vitest run src/features/cdr/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - CDR page renders with date range picker, customer/status/gateway filters
    - CDR table shows paginated results with sorting
    - Live calls tab shows active calls with 5s auto-refresh
    - Force hangup triggers red confirmation dialog and mutation
  </done>
</task>

<task type="auto">
  <name>Task 2: Build finance center -- transactions, rate plan management, profit analysis</name>
  <files>
    admin/src/features/finance/index.tsx,
    admin/src/features/finance/components/transaction-table.tsx,
    admin/src/features/finance/components/rate-plan-sheet.tsx,
    admin/src/features/finance/components/profit-analysis.tsx,
    admin/src/lib/api/hooks.ts
  </files>
  <action>
    1. Create transaction-table.tsx:
       - Columns: transaction ID, customer name, type (Badge: topup/deduction/call-charge/refund), amount (signed, color-coded: green positive, red negative), balance after, remark, created at
       - Server-side pagination, date range filter, customer filter, type filter
       - Summary row at top: total in, total out, net
       - Uses shared DataTable

    2. Create rate-plan-sheet.tsx:
       - shadcn Sheet for creating/editing rate plan templates
       - Fields: plan name, description, A-leg rate per minute (CNY), B-leg rate table (list of prefix -> rate pairs, add/remove rows), billing increment (seconds, default 6), minimum charge duration (seconds)
       - Dynamic B-leg prefix-rate list: add row button, each row has prefix input + rate input + remove button
       - react-hook-form + zod validation (rates must be positive numbers, prefixes must be numeric 3 digits)
       - Chinese labels

    3. Create profit-analysis.tsx:
       - Two Recharts BarChart panels side by side per user decision (split by customer and by gateway):
         a. By customer: Horizontal BarChart showing revenue, cost, and profit for top 10 customers. Stacked or grouped bars (revenue green, cost red, profit cyan).
         b. By gateway: Same layout but grouped by gateway
       - Date range selector (shared with transactions)
       - Total platform summary: total revenue, total cost, gross margin percentage
       - Uses CHART_COLORS, proper ResponsiveContainer, Chinese labels

    4. Create finance/index.tsx:
       - Tabs: "Transactions" (交易流水), "Rate Plans" (费率模板), "Profit Analysis" (毛利分析)
       - Transactions tab: TransactionTable
       - Rate Plans tab: DataTable of rate plans with edit/delete actions, "Create Rate Plan" button -> RatePlanSheet
       - Profit tab: ProfitAnalysis

    5. Update hooks.ts: Add finance hooks:
       - useTransactions({page, limit, dateRange, customerId, type})
       - useRatePlans({page, limit})
       - useCreateRatePlan() mutation
       - useUpdateRatePlan() mutation
       - useProfitByCustomer(dateRange)
       - useProfitByGateway(dateRange)
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/admin && npx tsc --noEmit && npx vitest run src/features/finance/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Finance transactions table renders with filters and pagination
    - Rate plan Sheet form validates prefix-rate pairs correctly
    - Profit analysis shows grouped bar charts by customer and gateway
    - All amounts formatted as CNY with proper sign coloring
  </done>
</task>

</tasks>

<verification>
- CDR and finance pages load with DataTables
- `cd admin && npx tsc --noEmit` passes
- `cd admin && npx vitest run --reporter=verbose` all tests pass
- Live calls refresh every 5 seconds
- Rate plan form supports dynamic prefix-rate list
- Profit charts render with proper grouping
</verification>

<success_criteria>
- CDR: Full query with 5 filter dimensions, live monitoring with force-hangup, tabbed layout
- Finance: Transaction ledger with filters, rate plan CRUD via Sheet, profit analysis by customer and gateway with Recharts
- Force-hangup uses red AlertDialog per user decision
- Rate plans support dynamic B-leg prefix-rate pairs
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard/04-05-SUMMARY.md`
</output>
