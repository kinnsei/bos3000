---
phase: 04-admin-dashboard
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - admin/src/features/did/index.tsx
  - admin/src/features/did/components/did-table.tsx
  - admin/src/features/did/components/did-assign-dialog.tsx
  - admin/src/features/compliance/index.tsx
  - admin/src/features/compliance/components/blacklist-table.tsx
  - admin/src/features/compliance/components/audit-log-table.tsx
  - admin/src/features/ops/index.tsx
  - admin/src/features/ops/components/fs-status-cards.tsx
  - admin/src/features/ops/components/system-health.tsx
  - admin/src/features/settings/index.tsx
  - admin/src/features/settings/components/config-editor.tsx
  - admin/src/lib/api/hooks.ts
autonomous: true
requirements:
  - ADMN-07
  - ADMN-08
  - ADMN-09
  - ADMN-10

must_haves:
  truths:
    - "Admin can view DID number list, bulk import via CSV, and assign/unassign DIDs to customers"
    - "Admin can manage global blacklist (view, add, bulk import CSV) and query audit logs"
    - "Admin can view FreeSWITCH instance status, ESL connection health, and system health metrics"
    - "Admin can view and edit system_configs key-value pairs through a visual form"
  artifacts:
    - path: "admin/src/features/did/index.tsx"
      provides: "DID management page"
      min_lines: 20
    - path: "admin/src/features/compliance/index.tsx"
      provides: "Compliance page (blacklist + audit)"
      min_lines: 20
    - path: "admin/src/features/ops/index.tsx"
      provides: "Operations monitoring page"
      min_lines: 20
    - path: "admin/src/features/settings/index.tsx"
      provides: "System settings page"
      min_lines: 20
  key_links:
    - from: "admin/src/features/did/index.tsx"
      to: "admin/src/components/shared/csv-upload.tsx"
      via: "CsvUpload for bulk DID import"
      pattern: "CsvUpload"
    - from: "admin/src/features/compliance/components/blacklist-table.tsx"
      to: "admin/src/components/shared/csv-upload.tsx"
      via: "CsvUpload for bulk blacklist import"
      pattern: "CsvUpload"
---

<objective>
Build four secondary management pages: DID number management (bulk import/assign), compliance (blacklist + audit logs), operations monitoring (FreeSWITCH/system health), and system settings (config editor).

Purpose: These complete the admin dashboard's operational coverage. DID and blacklist use the shared CsvUpload component for bulk imports. Ops monitoring provides system visibility. Settings enables runtime config changes.
Output: Four feature pages completing all ADMN requirements.
</objective>

<execution_context>
@/Users/kinnsei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kinnsei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-dashboard/04-CONTEXT.md
@.planning/phases/04-admin-dashboard/04-RESEARCH.md
@.planning/phases/04-admin-dashboard/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DID management and compliance pages (blacklist + audit logs)</name>
  <files>
    admin/src/features/did/index.tsx,
    admin/src/features/did/components/did-table.tsx,
    admin/src/features/did/components/did-assign-dialog.tsx,
    admin/src/features/compliance/index.tsx,
    admin/src/features/compliance/components/blacklist-table.tsx,
    admin/src/features/compliance/components/audit-log-table.tsx,
    admin/src/lib/api/hooks.ts
  </files>
  <action>
    **DID Management (ADMN-07):**

    1. Create did-table.tsx:
       - Columns: DID number, assigned customer (or "unassigned"), pool type (Badge: dedicated/shared), status (active/inactive), created date
       - Server-side pagination, filter by assignment status and customer
       - Row actions: assign to customer, unassign, deactivate

    2. Create did-assign-dialog.tsx:
       - Dialog for assigning a DID to a customer
       - Customer search/select dropdown
       - Pool type select: dedicated (exclusive) or shared (pool)
       - Submit calls useAssignDID() mutation

    3. Create did/index.tsx:
       - Title "DID Number Management" (Chinese)
       - Toolbar: "Bulk Import" button opens CsvUpload component (expected columns: number, pool_type), "Add DID" button opens single-add dialog
       - DidTable below
       - Summary stats: total DIDs, assigned count, unassigned count

    **Compliance (ADMN-08):**

    4. Create blacklist-table.tsx:
       - Columns: phone number, reason, added by (admin name), created date
       - Row actions: remove (red ConfirmDialog per user decision -- "Delete number X from blacklist?")
       - Toolbar: "Add Number" dialog (phone + reason), "Bulk Import" button opens CsvUpload (columns: number, reason)

    5. Create audit-log-table.tsx:
       - Columns: timestamp, operator (admin name), action type (Badge), resource type, resource ID, changes (expandable JSON diff showing before/after), IP address
       - Server-side pagination, filters: date range, operator, action type, resource type
       - Read-only -- no row actions
       - Expandable row to show full change details

    6. Create compliance/index.tsx:
       - Tabs: "Blacklist" (Chinese), "Audit Logs" (Chinese)
       - Blacklist tab: BlacklistTable with bulk import
       - Audit tab: AuditLogTable with filters

    7. Update hooks.ts: Add hooks for DID and compliance:
       - useDIDNumbers({page, limit, assignmentStatus, customerId})
       - useImportDIDs() mutation
       - useAssignDID() mutation
       - useUnassignDID() mutation
       - useBlacklist({page, limit})
       - useAddToBlacklist() mutation
       - useRemoveFromBlacklist() mutation
       - useImportBlacklist() mutation
       - useAuditLogs({page, limit, dateRange, operator, actionType, resourceType})
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/admin && npx tsc --noEmit && npx vitest run src/features/did/ src/features/compliance/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - DID page shows number table with assign/unassign dialogs
    - Bulk import DID via CsvUpload works (parse + preview + confirm flow)
    - Blacklist table shows numbers with add/remove/bulk-import
    - Audit log table renders with expandable change details
    - Remove from blacklist uses red ConfirmDialog
  </done>
</task>

<task type="auto">
  <name>Task 2: Build operations monitoring and system settings pages</name>
  <files>
    admin/src/features/ops/index.tsx,
    admin/src/features/ops/components/fs-status-cards.tsx,
    admin/src/features/ops/components/system-health.tsx,
    admin/src/features/settings/index.tsx,
    admin/src/features/settings/components/config-editor.tsx,
    admin/src/lib/api/hooks.ts
  </files>
  <action>
    **Operations Monitoring (ADMN-09):**

    1. Create fs-status-cards.tsx:
       - Two cards for FreeSWITCH instances (primary + secondary):
         - Each shows: hostname, ESL connection status (connected/disconnected badge), last health check time, active sessions count, uptime
         - Health indicator: green dot if healthy, red with pulse if unhealthy
       - Auto-refresh every 10 seconds

    2. Create system-health.tsx:
       - System resource cards grid:
         a. Database: connection pool stats (active/idle/max), response latency
         b. Redis: connected/disconnected, memory usage
         c. API: requests/min, error rate, avg latency
       - Each metric as a compact card with status badge and value
       - Overall system status badge at top (all green -> "Healthy", any red -> "Degraded")

    3. Create ops/index.tsx:
       - Title "Operations Monitoring" (Chinese)
       - FsStatusCards at top (full width)
       - SystemHealth below
       - Optionally: a mini log tail view showing last 10 system events (placeholder)

    **System Settings (ADMN-10):**

    4. Create config-editor.tsx:
       - Renders system_configs as a form with dynamic field types:
         - String configs: Input field
         - Number configs: Number input with min/max
         - Boolean configs: Switch toggle
         - JSON configs: Textarea with JSON validation
       - Each config shows: key, current value, description, last modified
       - Edit inline: click to edit, save button per row or save-all button
       - Uses react-hook-form for the form state

    5. Create settings/index.tsx:
       - Title "System Settings" (Chinese)
       - ConfigEditor component
       - Group configs by category if config keys have prefix pattern (e.g., "call.*", "billing.*", "compliance.*")

    6. Update hooks.ts: Add ops and settings hooks:
       - useFSStatus() with refetchInterval: 10000
       - useSystemHealth() with refetchInterval: 30000
       - useSystemConfigs()
       - useUpdateConfig() mutation
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/admin && npx tsc --noEmit && npx vitest run src/features/ops/ src/features/settings/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Ops page shows FS instance cards with health indicators and auto-refresh
    - System health cards display DB, Redis, API metrics
    - Settings page renders config editor with dynamic field types
    - Config saves trigger mutation and re-fetch
  </done>
</task>

</tasks>

<verification>
- All 4 feature pages load and render correctly
- `cd admin && npx tsc --noEmit` passes
- `cd admin && npx vitest run --reporter=verbose` all tests pass
- CsvUpload used correctly in DID and blacklist pages
- Ops auto-refresh works at 10s interval
</verification>

<success_criteria>
- DID: Table with assign/unassign, bulk CSV import with preview/validation
- Compliance: Blacklist CRUD with bulk import, audit log with expandable change details
- Ops: FreeSWITCH status cards with ESL health, system resource monitoring
- Settings: Dynamic config editor with type-appropriate input controls
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard/04-05-SUMMARY.md`
</output>
