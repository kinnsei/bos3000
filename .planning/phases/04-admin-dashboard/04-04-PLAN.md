---
phase: 04-admin-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - admin/src/features/customers/index.tsx
  - admin/src/features/customers/components/customer-table.tsx
  - admin/src/features/customers/components/customer-detail.tsx
  - admin/src/features/customers/components/create-customer-sheet.tsx
  - admin/src/features/customers/components/balance-dialog.tsx
  - admin/src/features/gateways/index.tsx
  - admin/src/features/gateways/components/gateway-table.tsx
  - admin/src/features/gateways/components/gateway-config-sheet.tsx
  - admin/src/features/gateways/components/gateway-health-badge.tsx
  - admin/src/lib/api/hooks.ts
autonomous: true
requirements:
  - ADMN-02
  - ADMN-03

must_haves:
  truths:
    - "Admin can view paginated customer list with sorting and filtering"
    - "Admin can create a new customer via side sheet form"
    - "Admin can top-up/deduct balance and freeze/unfreeze customers via dialogs"
    - "Admin can view customer detail page with API Key display"
    - "Admin can view A-leg and B-leg gateway pools with real-time health badges"
    - "Admin can toggle gateways online/offline and configure failover"
  artifacts:
    - path: "admin/src/features/customers/index.tsx"
      provides: "Customer list page"
      min_lines: 30
    - path: "admin/src/features/customers/components/customer-detail.tsx"
      provides: "Customer detail with balance, API keys, call stats"
      min_lines: 50
    - path: "admin/src/features/gateways/index.tsx"
      provides: "Gateway management page"
      min_lines: 30
  key_links:
    - from: "admin/src/features/customers/index.tsx"
      to: "admin/src/lib/api/hooks.ts"
      via: "useCustomers query hook"
      pattern: "useCustomers"
    - from: "admin/src/features/gateways/index.tsx"
      to: "admin/src/lib/api/hooks.ts"
      via: "useGateways query hook"
      pattern: "useGateways"
---

<objective>
Build the customer management module (list, create, detail, balance ops, freeze) and gateway management module (A/B pools, health status, online/offline toggle, failover config).

Purpose: These are the two core entity management pages admins use daily. Customer management covers the full lifecycle; gateway management enables real-time operations control.
Output: Two fully functional CRUD management pages with server-side DataTable, Sheet forms, and Dialog actions.
</objective>

<execution_context>
@/Users/kinnsei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kinnsei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-dashboard/04-CONTEXT.md
@.planning/phases/04-admin-dashboard/04-RESEARCH.md
@.planning/phases/04-admin-dashboard/04-01-SUMMARY.md
@.planning/phases/04-admin-dashboard/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build customer management -- list, create, detail, balance ops, freeze</name>
  <files>
    admin/src/features/customers/index.tsx,
    admin/src/features/customers/components/customer-table.tsx,
    admin/src/features/customers/components/customer-detail.tsx,
    admin/src/features/customers/components/create-customer-sheet.tsx,
    admin/src/features/customers/components/balance-dialog.tsx,
    admin/src/lib/api/hooks.ts
  </files>
  <action>
    1. Create customer-table.tsx:
       - Column definitions: company name, email, balance (formatted CNY), status (Badge: active/frozen), concurrent limit, daily limit, created date
       - Server-side pagination, sorting by balance/created date, text search filter on company name/email
       - Row actions dropdown: view detail, top-up, deduct, freeze/unfreeze
       - Uses shared DataTable component

    2. Create create-customer-sheet.tsx:
       - shadcn Sheet (side drawer) for complex create form per user decision
       - Fields: company name (required), email (required, validated), password, phone, concurrent limit (number, default 10), daily call limit (number, default 1000), initial balance (number, default 0)
       - Use react-hook-form + zod for validation
       - Submit calls useCreateCustomer() mutation, on success invalidates customer list and closes sheet
       - Chinese labels and validation messages

    3. Create balance-dialog.tsx:
       - shadcn Dialog for balance top-up or deduction (simple operation per user decision)
       - Mode prop: 'topup' or 'deduct'
       - Shows current balance, amount input, optional remark/note field
       - For deduction: red warning if amount > current balance
       - Submit calls useTopUpCustomer() or useDeductCustomer() mutation
       - Chinese labels

    4. Create customer-detail.tsx:
       - Route: /customers/:id
       - Top section: customer info card (company, email, status badge, dates)
       - Balance section: current balance display, credit limit, top-up/deduct buttons (open BalanceDialog)
       - API Key section: show key prefix (e.g., "bos_xxxx..."), copy full key button, regenerate button with ConfirmDialog
       - IP whitelist section: list of allowed IPs, add/remove
       - Recent calls summary: last 5 calls in a mini table
       - Freeze/unfreeze button at bottom with red ConfirmDialog for freeze action

    5. Create customers/index.tsx: Page wrapper with title "Customer Management" in Chinese, "Create Customer" button -> CreateCustomerSheet, and CustomerTable.

    6. Update hooks.ts: Add customer hooks with mock data:
       - useCustomers({page, limit, search, sortBy, sortOrder})
       - useCustomer(id) -- single customer detail
       - useCreateCustomer() mutation
       - useTopUpCustomer() mutation
       - useDeductCustomer() mutation
       - useFreezeCustomer() mutation
       - useCustomerApiKeys(userId) query
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/admin && npx tsc --noEmit && npx vitest run src/features/customers/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Customer list page renders DataTable with pagination and sorting
    - Create customer sheet opens with validated form
    - Balance dialog works for both top-up and deduction
    - Customer detail page shows all sections (info, balance, API keys, recent calls)
    - Freeze action uses red confirmation dialog
  </done>
</task>

<task type="auto">
  <name>Task 2: Build gateway management -- A/B pools, health badges, toggle, failover config</name>
  <files>
    admin/src/features/gateways/index.tsx,
    admin/src/features/gateways/components/gateway-table.tsx,
    admin/src/features/gateways/components/gateway-config-sheet.tsx,
    admin/src/features/gateways/components/gateway-health-badge.tsx,
    admin/src/lib/api/hooks.ts
  </files>
  <action>
    1. Create gateway-health-badge.tsx:
       - Reusable Badge component showing gateway health status
       - States: healthy (green dot + "Healthy"), unhealthy (red dot + "Unhealthy"), unknown (gray dot + "Unknown")
       - Subtle pulse animation on unhealthy state (CSS only)
       - Chinese labels

    2. Create gateway-table.tsx:
       - Two tabs: "A Gateway Pool" and "B Gateway Pool" (Chinese: "A 路网关池", "B 路网关池")
       - Columns: name, SIP address, type (A/B), health status (GatewayHealthBadge), weight (A-leg only), prefix (B-leg only), failover gateway, enabled status, concurrent calls
       - Row actions: toggle online/offline (Dialog confirmation), edit config (Sheet), test originate (Dialog with result display)
       - Toggle online/offline: simple Dialog per user decision ("confirm taking gateway X offline?")
       - Test originate: Dialog with phone number input, calls test API, shows result

    3. Create gateway-config-sheet.tsx:
       - shadcn Sheet for gateway configuration (complex operation per user decision)
       - Fields vary by type:
         - A-leg: name, SIP address, weight (1-100), max concurrent, enabled toggle
         - B-leg: name, SIP address, prefix (e.g., "130,131,132"), failover gateway select (dropdown of other B gateways), max concurrent, enabled toggle
       - Failover config: dropdown showing other gateways of same type
       - react-hook-form + zod validation
       - Chinese labels

    4. Create gateways/index.tsx: Page with title "Gateway Management" in Chinese, tab switcher for A/B pools, "Add Gateway" button -> GatewayConfigSheet, and GatewayTable.

    5. Update hooks.ts: Add gateway hooks with mock data:
       - useGateways({type: 'a'|'b', page, limit})
       - useToggleGateway() mutation
       - useCreateGateway() mutation
       - useUpdateGateway() mutation
       - useTestOriginate() mutation
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/admin && npx tsc --noEmit && npx vitest run src/features/gateways/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Gateway page shows tabbed A/B pool tables
    - Health badges show green/red/gray states with animation
    - Toggle online/offline shows confirmation dialog
    - Gateway config sheet opens with type-appropriate fields
    - Test originate dialog accepts phone number and displays result
  </done>
</task>

</tasks>

<verification>
- Customer and gateway pages load and display DataTables
- `cd admin && npx tsc --noEmit` passes
- `cd admin && npx vitest run --reporter=verbose` all tests pass
- Sheet forms validate input with zod schemas
- ConfirmDialog shows for dangerous actions (freeze, offline toggle)
</verification>

<success_criteria>
- Customer lifecycle fully manageable: list with search/sort/paginate, create via Sheet, detail with balance/keys/freeze
- Gateway pools viewable by type with health status, configurable via Sheet, toggle-able via Dialog
- All interaction patterns match user decisions: Dialog for simple ops, Sheet for complex config, red AlertDialog for danger
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard/04-04-SUMMARY.md`
</output>
