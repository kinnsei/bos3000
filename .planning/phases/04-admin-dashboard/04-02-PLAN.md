---
phase: 04-admin-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - admin/src/features/dashboard/index.tsx
  - admin/src/features/dashboard/components/overview-stats.tsx
  - admin/src/features/dashboard/components/alert-cards.tsx
  - admin/src/features/dashboard/components/trend-charts.tsx
  - admin/src/features/wastage/index.tsx
  - admin/src/features/wastage/components/wastage-trend.tsx
  - admin/src/features/wastage/components/customer-ranking.tsx
  - admin/src/features/wastage/components/failure-distribution.tsx
  - admin/src/lib/api/hooks.ts
autonomous: true
requirements:
  - ADMN-01
  - ADMN-05
  - WAST-03
  - UI-02

must_haves:
  truths:
    - "Dashboard page shows 4 stat cards (concurrent calls, today revenue, today wastage, bridge success rate) with animated number tickers"
    - "Dashboard shows 4 alert cards (low bridge rate, low balance customers, gateway DOWN, high wastage rate)"
    - "Dashboard data auto-refreshes every 30 seconds"
    - "Wastage analysis page shows 3 charts: trend line, customer TOP 10 bar, failure reason pie/bar"
    - "All charts use consistent Recharts theme with green/cyan/purple/orange/red palette"
  artifacts:
    - path: "admin/src/features/dashboard/index.tsx"
      provides: "Overview dashboard page"
      min_lines: 40
    - path: "admin/src/features/wastage/index.tsx"
      provides: "Wastage analysis center"
      min_lines: 40
    - path: "admin/src/features/dashboard/components/overview-stats.tsx"
      provides: "Stat cards with MagicUI NumberTicker"
      min_lines: 30
  key_links:
    - from: "admin/src/features/dashboard/index.tsx"
      to: "admin/src/lib/api/hooks.ts"
      via: "useDashboardOverview with 30s refetch"
      pattern: "useDashboardOverview"
    - from: "admin/src/features/wastage/index.tsx"
      to: "admin/src/lib/api/hooks.ts"
      via: "useWastageAnalysis hooks"
      pattern: "useWastage"
---

<objective>
Build the operations dashboard (overview page) with animated stat cards, alert notifications, and trend charts, plus the wastage analysis center with three visualization panels.

Purpose: The dashboard is the landing page and primary monitoring tool. Wastage analysis is a data-heavy visualization page that establishes the Recharts pattern for all subsequent chart usage.
Output: Two fully functional feature pages with professional data visualization.
</objective>

<execution_context>
@/Users/kinnsei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kinnsei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-dashboard/04-CONTEXT.md
@.planning/phases/04-admin-dashboard/04-RESEARCH.md
@.planning/phases/04-admin-dashboard/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build operations dashboard with animated stats, alert cards, and trend charts</name>
  <files>
    admin/src/features/dashboard/index.tsx,
    admin/src/features/dashboard/components/overview-stats.tsx,
    admin/src/features/dashboard/components/alert-cards.tsx,
    admin/src/features/dashboard/components/trend-charts.tsx,
    admin/src/lib/api/hooks.ts
  </files>
  <action>
    1. Copy MagicUI NumberTicker component from magicui.design into admin/src/components/ui/number-ticker.tsx. This is the copy-paste pattern (MagicUI is not an npm install). Requires framer-motion (already installed).

    2. Create overview-stats.tsx:
       - 4 StatCard instances in a responsive grid (4 cols on xl, 2 on md, 1 on sm):
         a. Concurrent calls (icon: PhoneCall, live count with NumberTicker animation)
         b. Today's revenue (icon: TrendingUp, formatted as CNY with NumberTicker)
         c. Today's wastage cost (icon: TrendingDown, formatted as CNY, red-tinted)
         d. Bridge success rate (icon: CheckCircle, percentage with NumberTicker)
       - Each card shows trend vs yesterday (up/down arrow + percentage)
       - Loading state: Skeleton bones

    3. Create alert-cards.tsx:
       - 4 alert cards in a row, each with status indicator (red/yellow/green dot):
         a. Bridge rate alert: Shows "normal" (green) or "alert: X% < threshold" (red pulse animation)
         b. Low balance customers: Count of customers below threshold (yellow if > 0)
         c. Gateway DOWN: Count of unhealthy gateways (red if > 0)
         d. Wastage rate alert: Shows "normal" or "alert: X% > threshold" (red)
       - Use subtle pulse animation (CSS @keyframes, not heavy framer-motion) for active alerts
       - Default thresholds: bridge rate < 60%, wastage rate > 15%

    4. Create trend-charts.tsx:
       - Two Recharts charts side by side (2 cols on lg, stack on md):
         a. Revenue trend: AreaChart with last 7 days, green fill gradient
         b. Call volume: BarChart with last 7 days, cyan bars
       - Use CHART_COLORS from lib/theme/chart-theme.ts
       - Wrap each chart in a div with explicit h-[280px] containing ResponsiveContainer
       - Tooltip with Chinese labels
       - Empty state: Centered text "loading..."

    5. Create dashboard/index.tsx composing all three:
       - Page title "Overview" in Chinese
       - OverviewStats at top
       - AlertCards below stats
       - TrendCharts at bottom
       - Data from useDashboardOverview() hook (30s auto-refresh via refetchInterval)
       - While backend not available, use realistic mock data in the hook (hardcoded fallback)

    6. Update hooks.ts: Flesh out useDashboardOverview() with proper query config and mock data fallback. Add useAlerts() hook.
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/admin && npx tsc --noEmit && npx vitest run src/features/dashboard/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Dashboard page renders stat cards with NumberTicker animation
    - Alert cards show status indicators
    - Recharts trend charts render with proper sizing and colors
    - Data auto-refreshes every 30s (verified by refetchInterval in hook)
    - All Chinese labels correct
  </done>
</task>

<task type="auto">
  <name>Task 2: Build wastage analysis center with trend, ranking, and failure distribution charts</name>
  <files>
    admin/src/features/wastage/index.tsx,
    admin/src/features/wastage/components/wastage-trend.tsx,
    admin/src/features/wastage/components/customer-ranking.tsx,
    admin/src/features/wastage/components/failure-distribution.tsx,
    admin/src/lib/api/hooks.ts
  </files>
  <action>
    1. Create wastage-trend.tsx:
       - Recharts LineChart showing wastage rate over time
       - Time range selector: tabs for day/week/month (per user decision)
       - Two lines: wastage rate (%) in red, bridge success rate (%) in green (for context)
       - X-axis: date labels, Y-axis: percentage
       - Tooltip with Chinese labels showing date, wastage rate, bridge rate
       - Proper ResponsiveContainer in h-[350px] parent

    2. Create customer-ranking.tsx:
       - Recharts horizontal BarChart showing TOP 10 customers by wastage cost
       - Each bar shows customer name + wastage amount (CNY)
       - Color gradient from red (highest) to orange (lowest in top 10)
       - Click on bar could navigate to customer detail (wire click handler, actual navigation)
       - h-[400px] container

    3. Create failure-distribution.tsx:
       - Recharts PieChart showing B-leg failure reason distribution
       - Categories: a_connected_b_failed, bridge_broken_early, b_no_answer, b_busy, b_rejected, other
       - Chinese labels for each category
       - Color palette from CHART_COLORS (different color per reason)
       - Legend below chart
       - h-[350px] container

    4. Create wastage/index.tsx:
       - Page title "Wastage Analysis Center" in Chinese
       - Layout: WastageTrend full width on top, CustomerRanking and FailureDistribution side by side below (2 cols on lg)
       - Summary stat row at top: total wastage cost today, wastage rate today, most common failure reason
       - Data from useWastageTrend(), useWastageRanking(), useWastageDistribution() hooks

    5. Update hooks.ts: Add wastage-specific query hooks with mock data fallback:
       - useWastageTrend(period: 'day'|'week'|'month')
       - useWastageRanking()
       - useWastageDistribution()
  </action>
  <verify>
    <automated>cd /Users/kinnsei/ai/bos3000/admin && npx tsc --noEmit && npx vitest run src/features/wastage/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Wastage page renders 3 distinct chart types (line, bar, pie)
    - Time range selector switches between day/week/month data
    - Customer ranking shows TOP 10 with horizontal bars
    - Failure distribution pie shows Chinese labels for each category
    - All charts use consistent CHART_COLORS palette
    - Recharts ResponsiveContainers properly sized (no 0-height issues)
  </done>
</task>

</tasks>

<verification>
- Dashboard page loads with stat cards, alerts, and charts
- Wastage page loads with three chart panels
- `cd admin && npx tsc --noEmit` passes
- `cd admin && npx vitest run --reporter=verbose` all tests pass
- Charts render at correct dimensions (no 0-height)
- All text in Chinese
</verification>

<success_criteria>
- Dashboard overview shows animated metric cards (NumberTicker), alert status cards, and trend charts
- Wastage analysis shows trend line (day/week/month), customer TOP 10 ranking, failure reason pie chart
- All Recharts usage follows consistent theme (CHART_COLORS, proper container sizing, Chinese tooltips)
- Data layer uses TanStack Query with 30s auto-refresh for dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard/04-02-SUMMARY.md`
</output>
